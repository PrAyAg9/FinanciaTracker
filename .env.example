# Finance Dashboard Environment Configuration

# ===========================================
# BACKEND CONFIGURATION
# ===========================================

# Server Configuration
NODE_ENV=development
PORT=3000
BACKEND_URL=http://localhost:3000
FRONTEND_URL=http://localhost:5173

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/finance-dashboard

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-123456789
JWT_EXPIRES_IN=7d

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# OpenAI Configuration (Optional - for AI transaction parsing)
OPENAI_API_KEY=your-openai-api-key-here

# Security
BCRYPT_ROUNDS=12

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================

# API Configuration
VITE_API_URL=http://localhost:3000

# Environment
VITE_NODE_ENV=development

# Feature Flags
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_AI_PARSING=true

# Google OAuth (Client ID only - secret stays on backend)
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here

# ===========================================
# INSTRUCTIONS
# ===========================================

# 1. Copy this file to .env in both root and backend directories
# 2. Copy frontend variables to frontend/.env
# 3. Replace all "your-*-here" values with your actual credentials
# 4. NEVER commit .env files to version control
# 5. For production, use environment variables instead of .env files

# ===========================================
# GETTING YOUR GOOGLE OAUTH CREDENTIALS
# ===========================================

# 1. Go to https://console.developers.google.com/
# 2. Create a new project or select existing one
# 3. Enable Google+ API
# 4. Go to "Credentials" section
# 5. Create "OAuth 2.0 Client IDs"
# 6. Set authorized origins: http://localhost:5173, http://localhost:3000
# 7. Set authorized redirect URIs: http://localhost:3000/auth/google/callback
# 8. Copy Client ID and Client Secret to this file

# ===========================================
# GETTING OPENAI API KEY (OPTIONAL)
# ===========================================

# 1. Go to https://platform.openai.com/
# 2. Sign up or log in
# 3. Go to API Keys section
# 4. Create new secret key
# 5. Copy the key to OPENAI_API_KEY above
# Note: This is optional - the app will work without it using simple parsing
